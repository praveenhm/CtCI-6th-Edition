package Q1_05_One_Away;

public class MySol {
    public static void main(String[] args) {

        String[] remove = {"pale", "ple"};
        oneWay(remove[0], remove[1]);
    }

    static boolean oneWay(String s1, String s2) {

        if (s1.length() < s2.length())
            return true;
        else if (s1.length() > 0) {

        }

        //remove check if it is one charcter removed
        if (s1.length() < s2.length()) {
            for (int i = 0; i < s1.length(); i++) {

            }

        }
        return true;
    }
}